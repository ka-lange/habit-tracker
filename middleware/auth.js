// module.exports = {
//     ensureAuth: function (req, res, next) {
//       if (req.isAuthenticated()) { //checks to see if you are authenticated
//         return next() //if you are, good, you can go to next page
//       } else {
//         res.redirect('/') //if not, no not good, takes back to home login page
//       }
//     }
//   }
  